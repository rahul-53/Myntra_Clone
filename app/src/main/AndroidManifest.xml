<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myntra_clone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myntra_Clone">
        <activity android:name=".SearchActivity"></activity>
        <activity android:name=".Notifications" />
        <activity android:name=".ShoppingBag" />
        <activity android:name=".TopWidget" />
        <activity android:name=".Login" />
        <activity android:name=".UpperDeals" />
        <activity android:name=".Home" />
        <activity android:name=".Profile" />
        <activity android:name=".Explore" />
        <activity android:name=".Studio" />
        <activity android:name=".Categories" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>